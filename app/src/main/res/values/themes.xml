<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.DS_Movies" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/Red</item>
        <item name="colorPrimaryVariant">#A50202</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
    
    <style name="AppEditTextStyle">
        <item name="android:textColor">@color/black</item>
        <item name="android:padding">10dp</item>
        <item name="android:textSize">12sp</item>
        <item name="android:textColorHint">@color/silver</item>
        <item name="android:fontFamily">@font/comic_sans</item>
        <item name="android:background">@drawable/shape_edit_text</item>
    </style>

    <style name="Theme.MovieTextStyle" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="android:textColor">@color/silver</item>
        <item name="fontFamily"> @font/comic_sans</item>
        <item name="android:textColorHint"> @color/silver</item>
        <!-- Customize your theme here. -->
    </style>
    <style name="Theme.LoginTextStyle" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Primary brand color. -->
<!--        <item name="android:textStyle">bold</item>-->
        <item name="android:textColor">@color/black</item>
        <item name="fontFamily"> @font/comic_sans</item>
        <item name="android:textSize">12sp</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.NoActionBar" parent="Theme.AppCompat.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
</resources>